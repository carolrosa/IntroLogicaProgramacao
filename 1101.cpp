#include <iostream>

using namespace std;

int main() {
    int m, n;
    cin >> m >> n;
    if (m>n);
        swap (m,n);
    for (i=m; i<=n; i++) {
        if (m<=0 and n<=0)
        cout << i << "Sum=" << soma << endl;
    }

    return 0;
}
